{
  /* inherit the default Next.js TypeScript rules  */
  "extends": "next",

  "compilerOptions": {
    /* ——— project-wide flags ——— */
    "strict": true,
    "forceConsistentCasingInFileNames": true,

    /* ——— path aliases ——— */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
      /* You may add this line later if an editor still complains:
         "next/image": ["components/LazyImage.tsx"]
      */
    }
  },

  /* keep TypeScript happy when scanning files */
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
