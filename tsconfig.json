{
  /* ——————————————————————————————————————————
     No "extends": Next 14 removed the old stubs
     —————————————————————————————————————————— */

  "compilerOptions": {
    /* 1. General safety / perf flags */
    "target": "esnext",
    "lib": ["dom", "dom.iterable", "esnext"],
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "incremental": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "skipLibCheck": true,

    /* 2. Path aliases */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],

      /*  Map every  import Image from "next/image"
          to your wrapper.  The directive keeps
          Next’s auto-rewriter from deleting it.           */
      // @ts-expect-error – custom alias needed for Lazy loading
      "next/image": ["components/LazyImage"]
    }
  },

  /* 3. Tell TS which files to scan */
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
