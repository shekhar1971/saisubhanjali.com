{
  /*
   * Use the official Next.js settings that live right inside
   * node_modules/next/tsconfig.json  →  always present in every build.
   */
  "extends": "./node_modules/next/tsconfig.json",

  "compilerOptions": {
    /* ── project flags ── */
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,

    /* ── path aliases ── */
    "baseUrl": ".",                   // allow "@/…" imports
    "paths": {
      "@/*": ["./*"]
      /*  we do NOT alias next/image here – webpack handles it */
    }
  },

  /* Let TS see generated files & your sources */
  "include": [
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": ["node_modules"]
}
