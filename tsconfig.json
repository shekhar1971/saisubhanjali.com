{
  // keep Next.js defaults (they add the "next" plugin automatically)
  "extends": "next/tsconfig.json",

  "compilerOptions": {
    /* strict-mode etc. – feel free to adjust */
    "strict": true,
    "forceConsistentCasingInFileNames": true,

    /* -------- PATH ALIASES ------------------------------------- */
    "baseUrl": ".",                 // allow "@/…" root imports
    "paths": {
      "@/*": ["./*"],               // (your existing alias)

      /* Map EVERY `import Image from "next/image"` to our wrapper.
         The ts-expect-error comment stops Next from deleting it. */
      // @ts-expect-error – custom alias required
      "next/image": ["components/LazyImage"]
    }
  },

  /* keep TS happy when scanning files */
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
