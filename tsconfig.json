{
  /* inherit sensible defaults & perf-focused rules from Next.js */
  "extends": "next/core-web-vitals",

  "compilerOptions": {
    /* ——— project-wide flags ——— */
    "strict": true,
    "forceConsistentCasingInFileNames": true,

    /* ——— path aliases ——— */
    "baseUrl": ".",                  // allow "@/…" absolute imports
    "paths": {
      "@/*": ["./*"]
      /* If your editor still complains about lazy images, you MAY add:
         "next/image": ["components/LazyImage.tsx"]
         It is optional and has no effect on the build. */
    }
  },

  /* keep TypeScript happy when scanning files */
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
