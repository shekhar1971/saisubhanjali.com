{
  /* inherit performant defaults from Next.js */
  "extends": "next/core-web-vitals",

  "compilerOptions": {
    /* ——— project-wide flags ——— */
    "strict": true,
    "forceConsistentCasingInFileNames": true,

    /* ——— path aliases ——— */
    "baseUrl": ".",          // allow "@/…" absolute paths
    "paths": {
      "@/*": ["./*"]
      /*  ⬅︎  we intentionally do NOT alias "next/image" here */
    }
  },

  /* keep TypeScript happy when scanning generated files */
  "include": [
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": ["node_modules"]
}
